<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.misiai.dao.UserMapper">
    <select id="findByUAndP" parameterType="com.misiai.entity.User" resultType="com.misiai.entity.User">
        select * from users where username=#{username} and password=#{password}
    </select>
    <!--<select id="findByU" -->
</mapper>